import React from 'react';
import { storiesOf } from '@storybook/react';

import Navigation from './navigation';

storiesOf('RTFKT/Components/Blocks/Navigation', module)
  .add('Default', () => (
    <Navigation/>
  ));
